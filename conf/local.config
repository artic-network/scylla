params {
    // Max resource options
    // Defaults only, expecting to be overwritten
    max_memory                 = '16.GB'
    max_cpus                   = 4
    max_time                   = '48.h'

    assembly_min_reads = 1
    assembly_min_percent = 0
}

params {
    // Max resource options
    // Defaults only, expecting to be overwritten
    max_memory                 = '16.GB'
    max_cpus                   = 4
    max_time                   = '48.h'

    sourmash_db_includes = "viral protozoa"
    raise_server = true
}
